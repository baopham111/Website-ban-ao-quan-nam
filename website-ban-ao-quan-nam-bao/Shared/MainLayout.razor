@inherits LayoutComponentBase

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <SearchBar /> <!-- Thêm thanh tìm kiếm -->
        <div class="content px-4">
            @Body
        </div>
    </main>
</div>

<style>
    .page {
        display: flex;
        flex-direction: row;
        min-height: 100vh;
    }

    .sidebar {
        width: 250px;
        background: linear-gradient(180deg, #1c1e21, #282c34);
        color: white;
        padding: 15px;
    }

    main {
        flex: 1;
        padding: 20px;
        background-color: #f5f5f5;
    }

    .content {
        padding: 20px;
        background-color: white;
        box-shadow: 0 0 10px rgba(0,0,0,0.1);
        border-radius: 10px;
    }
</style>
