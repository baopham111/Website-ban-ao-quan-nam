@* @page "/addproduct"
@using website_ban_ao_quan_nam_bao.Models
@using website_ban_ao_quan_nam_bao.Services
@inject ProductService ProductService
@inject NavigationManager Navigation

<h3 class="title">Thêm sản phẩm mới</h3>

<div class="form-group">
    <label for="name">Tên sản phẩm:</label>
    <input type="text" id="name" @bind="newProduct.Name" class="form-control" />
</div>
<div class="form-group">
    <label for="description">Mô tả:</label>
    <textarea id="description" @bind="newProduct.Description" class="form-control"></textarea>
</div>
<div class="form-group">
    <label for="price">Giá:</label>
    <input type="number" id="price" @bind="newProduct.Price" class="form-control" />
</div>
<div class="form-group">
    <label for="imageUrl">URL hình ảnh:</label>
    <input type="text" id="imageUrl" @bind="newProduct.ImageUrl" class="form-control" />
</div>
<button class="btn btn-primary" @onclick="AddProduct">Thêm sản phẩm</button>

@code {
    private Product newProduct = new Product();

    private void AddProduct()
    {
        ProductService.AddProduct(newProduct);
        Navigation.NavigateTo("/");
    }
}
 *@